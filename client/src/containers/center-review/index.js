import React from 'react';
import Layout from '../../components/Layout';
import FormFragment from './FormFragment';

export default () => {
  return (
    <Layout>
      <FormFragment />
    </Layout>
  );
};
